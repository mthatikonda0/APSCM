@model AGE.CMS.Data.Models.Intake.viewClientCMS

@{
    ViewBag.Title = "EditClientAssessement";
}



<div class="container">
    <b>Client Assessement </b>

    @using (Html.BeginForm("SaveClientAssessement", "Case", FormMethod.Post))
    {
        @Html.HiddenFor(model => model.Id)      
        @Html.HiddenFor(model => model.CaseheaderId)
        @Html.HiddenFor(model => model.viewClientAssessment.Id)  
        @Html.HiddenFor(model => model.IntakeId) 
        @Html.HiddenFor(model => model.IntakeDate)      
        <div class="well">
            <div class="row">
                <div class="col-lg-12 pull-right">
                    <div class="btn-group pull-right" style="margin-right: 10px;">
                        <button id="SaveClientassessement" rel="tooltip" type="submit" class="btn btn-success btn-md" title=" Save ">Save</button>
                        <a id="cancel" rel="tooltip" class="btn btn-default btn-md" title=" Cancel ">Cancel</a>
                    </div>
                </div>
            </div>

            <div class="row nextline">
                @Html.Partial("_ViewClientCMS", Model)
            </div>


            <div class="row nextline"></div>



            <div class="row">
                <div class="panel panel-default nextline">
                    <div class="panel-body ">
                        <div id="content">
                            <ul id="tabs" class="nav nav-tabs" data-tabs="tabs">
                                <li class="active"><a href="#tabs-A" data-toggle="tab">Indicators</a></li>
                                @* @if(Model.viewClientAssessment.mode == "summary")
                        {                        
                                <li><a href="#tabs-B" data-toggle="tab">Investigation Summary</a></li>                                 
                                <li><a href="#tabs-C" data-toggle="tab">Assessment Status and Substantiation Decision </a></li>
                        }*@
                            </ul>

                            <div id="managecontent" class="tab-content">
                                <div id="tabs-A" class="tab-pane active">
                                    @Html.Partial("DHSReferral", Model)
                                </div>
                                @* @if(Model.viewClientAssessment.mode == "summary")
                        {                        
                                <div id="tabs-B" class="tab-pane fade">
                                    @Html.Partial("InvestigationSummary", Model)
                    </div>
                                <div id="tabs-C" class="tab-pane fade">
                                    @Html.Partial("PartialAssessmentStatus", Model)
                    </div>
                        }*@
                            </div>
                        </div>
                    </div>
                </div>
            </div>




        </div>
        
    }
        
        
</div>
